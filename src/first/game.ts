import { Field, Board } from "./model";

const next = (b1: Board, b2: Board): Board => {
  
};

/*
- Generate Board with random fields.
- Display board (render it)

- Check alive neighbours around
- Based on the alive score -> next gen:[1|0]

*/


